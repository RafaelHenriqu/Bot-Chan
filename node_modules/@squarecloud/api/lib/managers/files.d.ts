/// <reference types="node" />
import { ApplicationFilesListResponse } from '../types';
import APIManager from './api';
export default class FilesManager {
    private readonly apiManager;
    private readonly appId;
    constructor(apiManager: APIManager, appId: string);
    /**  */
    list(path?: string): Promise<ApplicationFilesListResponse | undefined>;
    read(path: string): Promise<Buffer | undefined>;
    create(file: string | Buffer, fileName: string, path?: string): Promise<boolean>;
    delete(path: string): Promise<boolean>;
}
