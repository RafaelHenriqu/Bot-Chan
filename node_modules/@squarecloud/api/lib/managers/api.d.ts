import { APIRootPath, APIVersion, UserResponse } from '../types';
import { APIResponse } from '../types';
export default class APIManager {
    readonly apiKey: string;
    readonly baseUrl = "https://api.squarecloud.app";
    constructor(apiKey: string);
    user(userId?: string): Promise<APIResponse<UserResponse>>;
    application(path: string, appId: string, options?: RequestInit | 'GET' | 'POST' | 'DELETE'): Promise<APIResponse>;
    fetch(path: string, options?: RequestInit, version?: APIVersion<1 | 2>, rootPath?: APIRootPath): Promise<APIResponse>;
}
